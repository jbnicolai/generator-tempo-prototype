{
	"name": "<%= _.slugify(projectName) %>",
	"version": "0.1.0",
	"description": "<%= projectDescription %>",
	"main": "app/index.js",
  	"scripts": {
	    "postinstall": "bower install",
	    "prestart": "npm install",
  		"start": "node ./live-server.js"
  	},
	"dependencies": {
    	"live-server": "^0.6.2"		
		},
	"repository": {
	  "type": "git",
	  "url": "https://stash.t.is:7990/scm/tma/tempo-ux.git"
	},
	"author": "<%= projectAuthor %>",
	"license": "MIT"
}
